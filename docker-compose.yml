services:
  duckbot:
    image: duckbot:latest
    container_name: duckbot
    env_file:
      - .env
    environment:
      - PUID=1000
      - PGID=1000
      - PORT=3000
    ports:
      - 3000:3000
    networks:
      - csclub

networks:
  csclub:
    external: true
